# Capture Envelope Fixture Expectations

- `valid-minimal.json` — valid minimal `0.1-draft` Capture Envelope with required `version` and string `content`.
- `invalid-missing-content.json` — invalid because required `content` is absent.
