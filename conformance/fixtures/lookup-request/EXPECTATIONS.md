# Lookup Request Fixture Expectations

- `valid-minimal.json` — valid minimal `0.1-draft` Lookup Request with required `version` and non-empty `query`.
- `invalid-missing-query.json` — invalid because required `query` is absent.
