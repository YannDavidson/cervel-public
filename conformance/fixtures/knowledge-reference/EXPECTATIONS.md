# Knowledge Reference Fixture Expectations

- `valid-minimal.json` — valid minimal `0.1-draft` Knowledge Reference with required `version` and non-empty opaque `id`.
- `invalid-missing-id.json` — invalid because required `id` is absent.
