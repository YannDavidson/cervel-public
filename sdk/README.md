# CERVEL SDKs

Developer SDKs will provide typed access to deliberately public CERVEL interfaces without requiring applications to depend on production internals.

Planned initial language surfaces are TypeScript and Python. SDK releases will begin only after the corresponding public contracts and licensing terms are stable enough for external use.

SDKs should default toward least-privilege access, explicit identity/scope, safe handling of untrusted captured content, and traceable interactions.
