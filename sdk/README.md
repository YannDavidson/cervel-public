# CERVEL SDKs

This directory is reserved for developer tooling built against interfaces that have been explicitly approved and published in this repository.

SDK publication will follow the corresponding public contracts and licensing decisions. SDKs must not depend on or expose non-public implementation details.